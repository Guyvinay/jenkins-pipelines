pipeline {
    agent any

    tools {
        jdk 'tool-jdk-21'
        maven 'tool-maven'
    }

    stages {
        stage('Verify Tools') {
            steps {
                echo "Checking Java and Maven installations..."
                sh 'java -version'
                sh 'mvn -version'
            }
        }
    }
}

